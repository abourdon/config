#!/bin/bash

eval "$(jenv init -)"
export JAVA_HOME="$HOME/.jenv/versions/`jenv version-name`"
JAVA_BIN=$JAVA_HOME/bin

export NVM_DIR=$HOME/.nvm
source $(brew --prefix nvm)/nvm.sh

export PATH=$JENV_BIN:$JAVA_BIN:$PATH
